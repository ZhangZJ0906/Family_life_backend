services:
  backend-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    restart: always	  
    environment:
    - SERVER_PORT=8081

    networks:
      - family_net # 💡 加入網路

networks:
  family_net:
    name: family_shared_network # 💡 定義一個外部共享網路名稱